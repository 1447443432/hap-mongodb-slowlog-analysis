interface:
  display_name: "HAP MongoDB Slowlog Analysis"
  short_description: "Analyze MongoDB slow logs and suggest practical fixes"
  icon_small: "./assets/hap-mongodb-slowlog-analysis-small.svg"
  icon_large: "./assets/app-icon.svg"
  default_prompt: "Use HAP MongoDB Slowlog Analysis to analyze pasted or uploaded MongoDB slow logs, explain why they are slow, group repeated query shapes, and provide practical query rewrite and index guidance. Prefer default-value exact-match rewrites for null, empty string, empty array, and negative predicates before recommending ordinary indexes."
